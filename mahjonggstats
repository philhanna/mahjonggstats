#! /usr/bin/python3
from mj import History, HistoryLine, main, Main

import argparse

parser = argparse.ArgumentParser(description='mahjonggstats')
parser.add_argument('--name', '-n', help='select only this level name')
parser.add_argument('--level-names-only', '-l', action='store_true', help='show level names only')
parser.add_argument('--quiet', '-q', action='store_true', help='show level names and averages only')
kwargs = vars(parser.parse_args())

try:
    main = Main(**kwargs)
    main.run()
except ValueError as ex:
    print(ex)
except RuntimeError as ex:
    print(ex)
